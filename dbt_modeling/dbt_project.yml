name: 'sales_customer_project'
version: '1.0.0'

profile: 'sales_customer_project'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  sales_customer_project:
    +materialized: view 

    src:
      +materialized: ephemeral  

    dim:
      +schema: refined

    fct:
      +schema: refined

    marts:
      +schema: marts
